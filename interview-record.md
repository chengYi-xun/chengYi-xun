# 面试记录

---

## Q1: DAPO 和 GRPO 的区别是什么？什么情况下需要使用 DAPO？是什么问题导致需要使用 DAPO？

### GRPO 在大规模训练中暴露的三大问题

GRPO 的核心思想是"矮子里拔高个"——对同一个 Prompt 采样 G 个回答，用组内奖励的均值和标准差做标准化，得到相对优势 $\hat{A}_i = \frac{r_i - \mu_R}{\sigma_R + \varepsilon}$。这在中小规模训练中工作良好，但在大规模推理 RL（比如训练数学竞赛模型）时遇到了三个关键问题：

**问题一：有效样本稀疏。** 随着训练推进，模型逐渐"两极化"——简单题全对、困难题全错。此时一组 G 个回答的奖励完全相同（$\sigma_R = 0$），优势 $\hat{A}_i = 0$，梯度信号为零。batch 中大部分 Prompt 变得无效，计算资源被白白浪费。

**问题二：熵崩溃（Entropy Collapse）。** GRPO 使用对称裁剪 $[1-\varepsilon, 1+\varepsilon]$，对低概率的探索性 Token 极不友好。概率仅 0.01 的 Token 每步最多涨到 $0.01 \times 1.2 = 0.012$，而高概率 Token（0.5）能轻松涨到 0.6。对称裁剪系统性地压制低概率路径，策略熵持续下降，模型最终只会输出少数固定模式的回答。

**问题三：长回答的梯度稀释。** GRPO 使用序列级归一化——每个回答不论长短权重都相同。一个 5000 Token 的长推理中，每个 Token 分到的梯度只有 50 Token 短回答的 1/100，有效信号被严重稀释。

### DAPO 的四大技术改进

**技术一：Clip-Higher（非对称裁剪）**

将裁剪范围从对称 $[1-\varepsilon, 1+\varepsilon] = [0.8, 1.2]$ 改为非对称 $[1-\varepsilon_{\text{low}}, 1+\varepsilon_{\text{high}}] = [0.8, 1.28]$。

裁剪目标的梯度行为：只要概率比 $r_t$ 没超过上界，梯度正常流过、概率继续涨；一旦触顶，梯度变零。非对称设计给了低概率探索路径更大的增长空间——经过 10 轮更新，对称裁剪下 0.01 的 Token 只能涨到 0.062，而 Clip-Higher 能涨到 0.112，差距接近 2 倍。

对于 $\hat{A}_t < 0$ 的情况（坏回答中的 Token），裁剪下界仍由 $\varepsilon_{\text{low}} = 0.2$ 控制——惩罚力度不变。不对称设计的本质是：给好的探索更多增长空间，对错误保持同等惩罚。

**技术二：Dynamic Sampling（动态采样）**

只保留组内奖励方差大于零（$\sigma_R > 0$）的 Prompt 组，过滤掉全对或全错的无效样本，然后累积有效样本直到达到目标 batch size。核心约束是每组中必须同时包含正确和错误的回答：$0 < |\{o_i \mid \text{is\_correct}(a, o_i)\}| < G$，确保双向梯度信号。

**技术三：Token-Level Policy Gradient Loss（Token 级损失）**

归一化因子从"回答数"改为"总 Token 数"：

$$\text{GRPO}: \frac{1}{G}\sum_{i=1}^G\left(\frac{1}{|o_i|}\sum_t L_{i,t}\right) \quad \rightarrow \quad \text{DAPO}: \frac{1}{\sum_{i=1}^G|o_i|}\sum_{i=1}^G\sum_t L_{i,t}$$

每个 Token 的贡献相等，长回答中的推理步骤得到了应有的梯度信号。

**技术四：Overlong Reward Shaping（超长回答奖励塑形）**

对超过阈值 $L_{\text{thresh}} = L_{\text{max}} - L_{\text{buffer}}$ 的回答施加线性长度惩罚：

$$r_{\text{shaped}} = \begin{cases} r_{\text{original}} & \text{if } |o| \leq L_{\text{thresh}} \\ \min(r_{\text{original}},\ -\frac{|o| - L_{\text{thresh}}}{L_{\text{buffer}}} \cdot p) & \text{if } |o| > L_{\text{thresh}} \end{cases}$$

迫使模型学习更简洁高效的推理路径。

### DAPO 还移除了 KL 散度正则项

GRPO 中 KL 惩罚 $\beta \cdot D_{\text{KL}}(\pi_\theta \| \pi_{\text{ref}})$ 的职责是防止 reward hacking。但 KL 惩罚是全局性的，它惩罚所有概率变化包括有益探索，加剧熵崩溃。Clip-Higher 是局部性约束——只在概率比超出范围时截断，范围内完全自由。裁剪机制已足够约束策略，移除 KL 还能省掉冻结参考模型的显存。

### 什么时候需要用 DAPO

DAPO 主要面向**大规模 LLM 推理 RL** 场景，尤其是：

1. 奖励信号为稀疏二值（如数学题的对/错），容易出现组内全对或全错的情况
2. 模型参数量大（数十亿级），对显存敏感，不想额外维护 Reference Model
3. 回答长度差异极大（推理任务中 50~5000+ Token 都有）
4. 需要保持策略多样性，避免模型坍缩到单一解法

### DAPO 与 GRPO 差异对比

| 特性 | GRPO | DAPO |
|:---|:---:|:---:|
| 裁剪方式 | 对称 $[1-\varepsilon, 1+\varepsilon]$ | 非对称 $[1-\varepsilon_{\text{low}}, 1+\varepsilon_{\text{high}}]$ |
| 采样策略 | 固定 batch，包含零方差组 | 动态采样，过滤零方差组 |
| 损失归一化 | 序列级（每个回答等权） | Token 级（每个 Token 等权） |
| 长度控制 | 无 | 超长奖励惩罚 |
| KL 正则 | $\beta \cdot \text{KL}(\pi_\theta \| \pi_{\text{ref}})$ | 移除 KL（依赖裁剪约束策略） |

**总结：GRPO 解决了"去掉 Critic"的问题，DAPO 解决了"GRPO 在大规模训练中工程落地"的问题。**

---

## Q2: Flow-GRPO 和 MixGRPO 有什么区别？

### 核心区别概述

Flow-GRPO 和 MixGRPO 解决的是同一个问题的不同层面——将 GRPO 应用于图像/视频生成模型的在线 RL 训练。Flow-GRPO 是**开创者**，解决了"如何定义 $\log\pi_\theta$"这个根本性问题；MixGRPO 是**效率优化者**，解决了"全轨迹 SDE 计算开销太大"的工程瓶颈。

### Flow-GRPO 的核心贡献

Flow-GRPO 解决的关键问题是：**在连续生成过程中如何定义策略的对数概率？**

在 LLM 中，动作是离散 Token，$\log\pi_\theta$ 就是 softmax 输出的对数概率——定义清晰。但 Flow Matching 的生成过程是一个**连续的常微分方程（ODE）求解**，没有天然的"离散动作"概念。确定性 ODE 给定初始噪声后每步转移唯一确定，不存在"选择 A 而非 B"的随机性，没有 $\log\pi_\theta$ 可以计算。

**Flow-GRPO 的三步解决思路**：

1. **引入随机性（ODE→SDE）**：在 ODE 的每一步注入高斯噪声，将确定性 ODE 改为随机 SDE。每步转移变成了一个高斯分布
2. **定义对数概率**：$x_{t-\Delta t} \sim \mathcal{N}(\mu_\theta(x_t, t), \sigma^2 I)$，从而 $\log\pi_\theta$ 就是这个高斯的对数概率密度
3. **累积整条轨迹的对数概率**用于 GRPO 更新

Flow-GRPO 还将去噪过程建模为 MDP：状态 $s_t = (x_t, t, c)$，动作 $a_t = v_\theta(x_t, t, c)$，转移是 SDE 步，奖励仅在 $t=0$ 时由 RM 给出（稀疏奖励）。

### MixGRPO 解决的问题

Flow-GRPO（以及后续的 DanceGRPO）有两个核心效率瓶颈：

**问题一：全轨迹 SDE 的算力浪费 + 不必要的随机性。** Rollout 阶段必须在 T 步去噪的每一步注入 SDE 噪声以满足 MDP 建模，导致无法使用 DPM-Solver++ 等高阶 ODE 求解器加速数据收集。更根本的是，生成过程具有阶段性（早期定构图、后期修细节），却在所有时间步都注入探索噪声——在不需要探索的步上浪费了算力。

**问题二：DanceGRPO 随机抽样的副作用。** DanceGRPO 通过随机抽 K 步优化来省显存，但未被选中的步上的错误探索无法纠正；同时早期构图步与后期细节步的梯度方向可能冲突。实验证明：优化步数从 14 降到 4 时，ImageReward 下降 7%。

### MixGRPO 的核心设计

MixGRPO 的根本观察：**并非所有步都需要是随机的。** ODE 步是确定性的，对 MDP 贡献零信息量却消耗大量计算。因此将 MDP 缩短到仅包含 SDE 步的子区间。

**关键贡献一：混合 ODE-SDE 采样**

在去噪时间线上定义滑动窗口 $S = [t_l, t_r)$，窗口内用 SDE 探索，窗口外用确定性 ODE：

$$dx_t = \begin{cases} (v_\theta - \frac{1}{2}\sigma_t^2 s_t)dt + \sigma_t dw, & t \in S \\ v_\theta dt, & \text{otherwise} \end{cases}$$

GRPO 优化范围缩小为仅 S 内的步。通过 Fokker-Planck 方程证明：混合 ODE-SDE 与纯 ODE 产生**完全相同的边缘分布**。

**关键贡献二：滑动窗口作为课程学习**

窗口随训练从低 SNR（噪声端，构图阶段）向高 SNR（干净端，细节阶段）移动。这等价于隐式课程学习——先优化全局构图，再优化纹理细节。消融实验验证 Progressive 一致优于 Random，证明课程学习优于随机抽样。

**关键贡献三：高阶求解器加速（MixGRPO-Flash）**

窗口外的 ODE 步可以用 DPM-Solver++ 加速！MixGRPO-Flash* 将 NFE 从 25 降到 8，训练耗时从 DanceGRPO 的 9.30s/图降到 3.79s/图（降 60%），性能反而更优。

### 完整对比

| 设计 | Flow-GRPO / DanceGRPO | MixGRPO |
|:---|:---|:---|
| 噪声注入范围 | 全轨迹 T 步都是 SDE | 仅滑动窗口 $S=[t_l, t_r)$ 内是 SDE，窗口外是 ODE |
| 优化范围 | 全轨迹（或随机抽样 K 步） | 仅窗口内的 SDE 步 |
| 高阶求解器 | 不可用（全程有噪声） | 窗口外 ODE 步可用 DPM-Solver++ 加速 |
| 课程学习 | 无（随机抽样） | 滑动窗口从低 SNR→高 SNR |
| 采样步数（NFE） | 25 步 | 低至 8 步（MixGRPO-Flash*） |
| 训练耗时 | 基线 | 降至 29%~50% |

**总结：Flow-GRPO 回答了"能不能做"（理论可行性），MixGRPO 回答了"怎么做得又好又快"（工程效率）。**

---

## Q3: 什么是 CPS？

### CPS（Coefficients-Preserving Sampling，系数保持采样）

CPS 是针对 Flow-GRPO/DanceGRPO 中**标准 SDE 采样产生颗粒感伪影**问题而提出的替代采样方法（来自 Flow-CPS, arXiv:2509.05952）。由于效果极佳，被迅速集成到 MixGRPO 等各大开源代码库中。

### 标准 SDE 采样的问题

在 Flow-GRPO 中，为了给 RL 提供探索空间，需要将确定性 ODE 改为 SDE（注入随机噪声）。标准 SDE（Euler-Maruyama 离散化）的做法是：

$$x_{t_{i-1}} = x_{t_i} - v_{t_i} \Delta t + \sqrt{2\sigma^2 \Delta t}\,\epsilon_i - \frac{1}{2}\sigma^2 s_{t_i}(x_{t_i}) \Delta t$$

这里是"强行加噪 + Score 修正"的策略：强行叠加一个独立噪声让模型探索，然后引入基于 Score Function 的漂移修正项来保证总方差不变。

但 Score 靠 Tweedie 公式估算（$s_t \approx -(x_t - \hat{x}_0)/\sigma_t^2$），存在：

- 后验均值坍缩
- 模型低频偏好
- 大步长离散化误差

这些误差导致高频噪声残留 → 颗粒感伪影 → "欺骗"对纹理敏感的代理 RM（Reward Hacking）。

### CPS 的核心思路

CPS 借鉴 DDIM 的**待定系数法**，完全绕开显式 Score 估算。

假设下一步状态是当前状态、模型预测速度场和新噪声的线性组合：

$$x_{t_{i-1}} = A \cdot x_{t_i} + B \cdot v_{t_i} + C \cdot \epsilon_i$$

利用 Flow Matching 的理论路径 $x_t = (1-t)x_0 + t\epsilon$，将 $x_{t_i}$ 和 $v_{t_i}$ 展开为 $x_0$ 和 $\epsilon$ 的形式代回方程，**强制要求** $x_{t_{i-1}}$ 精确服从目标时刻的边缘分布 $x_{t_{i-1}} = (1-t_{i-1})x_0 + t_{i-1}\epsilon$。通过对比两边 $x_0$ 和 $\epsilon$ 的系数（设定 $C = \sigma_{t_i}$），解析解出 $A$ 和 $B$。

最终得到 CPS 采样公式：

$$x_{t_{i-1}} = \underbrace{(1-t_{i-1})\hat{x}_0 + \sqrt{t_{i-1}^2 - \sigma_{t_i}^2} \cdot \hat{\epsilon}}_{\mu\text{（均值）}} + \underbrace{\sigma_{t_i} \cdot \epsilon_i}_{\text{注入噪声}}$$

其中：

- $\hat{x}_0 = x_{t_i} - t_i \cdot v_{t_i}$（模型预测的干净图像）
- $\hat{\epsilon} = x_{t_i} + (1-t_i) v_{t_i}$（模型预测的噪声）

代码实现极简：

```python
# 1. 计算均值 μ
prev_sample_mean = pred_original_sample * (1 - sigma_prev) + noise_estimate * math.sqrt(sigma_prev**2 - std_dev_t**2)
# 2. 加噪声
prev_sample = prev_sample_mean + std_dev_t * variance_noise
```

### 标准 SDE vs CPS 的本质区别

| | 标准 SDE | CPS |
|:---|:---|:---|
| 保证分布一致的方式 | 加噪后用 Score Function **事后修正** | 通过系数约束**从设计上保证** |
| Score 估算 | 需要（Tweedie 近似，有误差） | 不需要 |
| 伪影问题 | 有颗粒感（高频噪声残留） | 干净（系数精确保持） |
| 数学保证 | 连续极限下精确，离散化有误差 | 每步都精确保持边缘分布的均值和方差 |

**总结：CPS 用待定系数法直接解出保持分布的采样公式，绕过了 Score 估算这个"不稳定中间商"，从根源上消除了 SDE 采样的伪影问题。**

---

## Q3 追问：标准 SDE 伪影的根因简述

标准 SDE 每一步做两件事：

1. **注入噪声** $\sigma\epsilon$：白噪声，能量均匀分布在所有频率
2. **Score 修正**：目的是把加的噪声"拉回来"，保持分布不变

Score 修正力正比于 $(x_t - \hat{x}_0)$。而 $\hat{x}_0$（后验均值）是模型对干净图像的**加权平均估计**，天然丢失高频信息（平均会抹掉细节）。

因此：

- **低频**：$\hat{x}_0$ 预测准确 → 残差几乎只含噪声 → Score 精准补偿 → 干净
- **高频**：$\hat{x}_0$ 预测为零 → 残差里信号和噪声混合 → Score 无法区分 → 修正不准 → 噪声残留

**一句话根因**：Score 来自后验均值 → 后验均值丢失高频 → 高频噪声修正不足 → 逐步累积 → 颗粒感。

**比喻**：撒了全频粉笔灰（白噪声），用粗布擦（Score 修正）——大面积灰擦干净了（低频），细颗粒擦不到（高频），多轮后细颗粒越积越多。

---

## Q3 再追问：哪里注入了高频噪声？

"高频噪声"不是单独注入的，而是**每步 SDE 注入的高斯白噪声 $\epsilon \sim \mathcal{N}(0,I)$ 本身就包含所有频率的分量**。

白噪声的频谱特征是各频率能量均等——可以对 $\epsilon$ 做傅里叶变换来理解：

- 低频分量：大面积的缓慢变化（整体亮度波动）
- 高频分量：像素级的快速变化（颗粒状点噪声）

这两者都在同一个 $\epsilon$ 里，只是频率不同。

更准确的表述：**每步注入的白噪声包含所有频率分量 → Score 修正能有效抵消低频分量但对高频力不从心 → 高频分量逐步累积 → 颗粒感伪影。** 不是"专门注入了高频噪声"，而是"注入的噪声里本来就有高频成分，修正机制只擦掉了低频那部分"。

---

## Q4: 什么是世界模型？

### 定义

世界模型是一个能够预测环境在你的动作下会如何变化的神经网络——本质上是大脑中"物理模拟器"的计算实现。

### 认知科学起源

Kenneth Craik (1943)："如果生物体能在头脑中构建一个外部现实的微型模型，它就能在行动前先在模型中尝试各种方案，预测哪种最优。"

直觉例子：闭上眼睛想象踢一个足球——球从脚尖飞出，画抛物线，弹地后减速。你不需要真看到球，就能在脑中预测轨迹。这种能力就来自你大脑中的世界模型。

### 核心架构（四大组件）

无论技术路线如何变化，世界模型大多包含四个核心组件：

1. **推断模型（编码器）**：从观测画面反推世界的真实状态，将复杂像素压缩为低维潜状态（Latent State）
2. **动力学模型（模拟器）**：给定当前状态和动作，向前推演下一步——这是核心引擎，能纯凭想象推演未来
3. **观测模型（解码器）**：把推演出的状态重新渲染成像素画面，检验模型是否"瞎想"
4. **奖励模型（裁判）**：评估推演出的未来局面对自己有多好，帮助选出最佳方案

### 为什么在"潜空间"做梦

不直接预测像素，原因有二：

- **维度太高**：一帧画面上万像素，绝大多数是无关紧要的废话细节（风吹草动、光影变化），压缩到潜空间只保留核心语义（"前面有辆车在左转"）
- **误差滚雪球**：连续预测 50 步，像素级预测每步误差会累积到画面完全模糊，低维空间里只有几十个核心数字，误差累积极慢

### 两大路线之争：预测派 vs 生成派

截至 2026 年，世界模型的核心分歧只有一条：**世界模型到底需不需要"看得见"（重建像素）？**

**预测派（Predictive）—— 决策不需要看清细节**

- 认为真实世界有太多不可预测的废话细节，强行预测每个像素纯属浪费算力
- 只预测"世界未来的抽象状态（潜变量）"，只要符合物理规律能用来做决策就行
- **Model-based RL**：代表作 **Dreamer** 系列，在抽象空间做梦训练策略
- **JEPA 架构**：Yann LeCun 主导的 **V-JEPA**，连解码器都不要了，直接预测未来的抽象特征

**生成派（Generative）—— 完美重建即完美理解**

- 坚信只要模型能完美生成高保真的未来视频，它内部就必然已经学懂了物理规律
- **视频生成即世界模拟器**：代表作 **Sora、Genie 3、Cosmos**
- **物理化世界模型（进阶补丁）**：往生成模型里强行注入物理常识（外挂物理引擎、用大模型当物理裁判、用 RL 对齐物理定律）

### 2026 前沿：万物归一

Var-JEPA 给出了数学证明：两派共享**同一套变分推断框架**，区别仅在于一个旋钮——**观测噪声的大小**。噪声大时需要概率分布 + 解码器（生成派），噪声趋近零时分布坍缩为确定性点、解码器多余（预测派/JEPA）。两者不过是同一数学框架在不同参数下的表现。

### 经典起源：Ha & Schmidhuber (2018)

三模块复现人类思维：

- **V 模型（Vision，看）**：VAE 压缩像素为低维向量
- **M 模型（Memory，想）**：RNN 预测低维向量的变化
- **C 模型（Controller，做）**：极简控制器做决策

最惊艳实验：完全在 M 模型生成的"梦境"中训练控制器，丢回真实游戏直接通关——证明了"学懂规律后可以在想象中无限练习"。

---

## Q5: 世界模型在自动驾驶或机器人中如何使用？

### 为什么自动驾驶特别需要世界模型

传统方案有两个痛点：

- **真实路测**：长尾场景（如行人突然冲出）几万公里也遇不到，有安全风险
- **传统仿真器**（如 CARLA）：画面和真实世界差距大（domain gap），策略难以迁移

世界模型提供了**第三条路**：从真实驾驶视频中学习环境动力学，生成无限的、逼真的、可控的驾驶场景。

自动驾驶对世界模型有三个独特需求：3D 几何理解、精确可控（根据驾驶指令生成场景）、安全可靠。

### 自动驾驶中的三种应用范式

**范式一：像素空间预测（GAIA-1、DriveDreamer、Vista）**

将世界模型定位为"场景生成器"：

- **GAIA-1**（9B 参数，Wayve）：用 Transformer 自回归预测视频 token，从 4700 小时驾驶视频中涌现 3D 理解和交通规则。缺点：可控性差
- **DriveDreamer**（GigaAI/清华）：引入结构化中间表示（HD Map + 3D Bbox + 交通灯状态），两阶段流程"预测布局→渲染像素"，实现精确可控。DriveDreamer-2 还引入 LLM 作为交通导演推理合理行为
- **Vista**（上海AI Lab）：跨数据集通用驾驶世界模型，独特贡献是**首次利用世界模型自身建立通用奖励函数**，无需真值数据即可评估驾驶动作质量

**范式二：3D 占用空间预测（OccWorld）**

完全跳过像素，直接在 3D 体素网格中预测世界变化：

$$\hat{\mathcal{O}}_{t+1} = \text{OccWorld}(\mathcal{O}_{1:t}, a_t)$$

$\mathcal{O} \in \{0,1,...,C\}^{200\times200\times16}$ 覆盖车辆周围 100m×100m×8m，每个体素标注占据类别。用 GPT 式 Transformer 自回归预测 VQ 编码后的体素 token。

优势：输出直接用于规划避障，不需要再过感知模块。且能区分"桥下的空间可以通过"和"前方墙壁不可通过"——BEV 无法做到的。

**范式三：世界模型作为策略训练环境**

最有价值的应用：完全在世界模型的"想象"中训练驾驶策略，然后迁移到真实世界。

$$\pi^* = \arg\max_\pi \mathbb{E}_{\text{WorldModel}}[\sum_t r_t]$$

Vista 的增强流程：真实数据 → 训练世界模型 → 生成数据 + 奖励信号 → 训练策略 → 部署真车。

### 机器人中的应用

核心思路一致——在想象中练习，减少真实交互成本：

1. **想象中规划（MPC）**：给定当前状态，在世界模型中模拟多种动作序列的后果，选最优路径
2. **数据增广**：真实机器人交互数据昂贵且危险，用世界模型生成大量虚拟经验训练策略
3. **安全验证**：真实执行前先在世界模型中检验动作安全性

Dreamer 系列是最成功的范例——用 RSSM 在潜空间做梦，训练出的策略可直接控制真实机器狗。

### 核心挑战

- **Sim2Real 鸿沟**：想象中学到的策略能否无缝迁移到嘈杂的真实世界
- **长期一致性**：当前模型在长时间跨度的想象预测上仍易崩溃
- **因果 vs 相关**：模型是否真正掌握了因果推理，还是仅是数据的相关性插值

---

## Q6: 生成模型中的流匹配（Flow Matching）是什么？

### 一句话定义

Flow Matching 是一种生成模型的训练方法，通过学习一个**时间相关的速度场** $v_t(x)$，指导样本沿连续路径从噪声分布流向数据分布。

### 核心思想

图像生成的本质问题：如何把标准高斯 $\mathcal{N}(0,I)$ 转换为数据分布 $p_\text{data}(x)$？

与 Diffusion Model 学习"逐步去噪"不同，Flow Matching 直接建模两个分布之间的连续传输过程——想象一群粒子从噪声位置出发，沿某个流场，流向数据分布的位置。模型学的就是这个流场在每个位置、每个时刻的速度方向。

### 数学框架

**路径定义**：对噪声 $x_0 \sim \mathcal{N}(0,I)$ 和数据 $x_1 \sim p_\text{data}$，用线性插值连接：

$$x_t = (1-t)x_0 + t \cdot x_1, \quad t \in [0, 1]$$

**真实速度**：对这条直线路径求导，真实速度是常数：

$$\frac{dx_t}{dt} = x_1 - x_0$$

**训练目标**：让神经网络 $v_\theta(x_t, t)$ 预测这个真实速度：

$$\mathcal{L}_\text{FM} = \mathbb{E}_{t, x_0, x_1}\left[\|v_\theta(x_t, t) - (x_1 - x_0)\|^2\right]$$

**生成过程**：从 $x_0 \sim \mathcal{N}(0,I)$ 出发，用 ODE 求解器迭代：

$$x_{t+\Delta t} = x_t + \Delta t \cdot v_\theta(x_t, t)$$

### 与 Diffusion Model 的关系

| | Diffusion Model | Flow Matching |
|:---|:---|:---|
| 前向过程 | 逐步加噪（马尔可夫链） | 噪声到数据的线性插值 |
| 学习目标 | 预测噪声 $\epsilon$（或 Score） | 预测速度场 $v_t$ |
| 路径形状 | 弯曲（方差按 schedule 变化） | 直线（最简单的连接） |
| 生成过程 | 反向 SDE 或 ODE | 正向 ODE |
| 采样效率 | 通常需要更多步 | 直线路径天然需要更少步 |

两者本质是统一的：Diffusion 只是 Flow Matching 在特定（弯曲）路径下的特例。Flow Matching 的直线路径使得 ODE 轨迹几乎不交叉，高阶求解器可用更少步数完成积分。

### 背景知识：连续正规化流（CNF）

Flow Matching 可以看作 Normalizing Flow 的连续推广。CNF 将离散变换替换为 ODE：

$$\frac{d\mathbf{z}_t}{dt} = v(\mathbf{z}_t, t)$$

一旦学到向量场 $v$，就可以用迭代法求解实现生成。Flow Matching 就是训练这个向量场的具体方法。

### 为什么 SD3 和 Flux 用 Flow Matching

1. **训练更简单**：损失函数就是 MSE 预测速度，不需要复杂的噪声 schedule 设计
2. **采样更快**：直线路径 + ODE 求解器，少步数即可生成高质量图像
3. **数学更优雅**：统一了 Normalizing Flow 和 Diffusion 的理论框架

---

## Q7: Rectified Flow 和 Flow Matching 有什么区别？

### 本质关系：接口 vs 实现

**Flow Matching 是一套通用理论框架（"接口"）**，它定义了"如何通过回归速度场来训练连续正规化流"，但不限制数据到噪声的概率路径长什么样（可以是弯曲的、复杂的）。

**Rectified Flow 是这个框架下最简单、最暴力的一个具体实现（"实例"）**。

类比：Flow Matching 是"排序算法"这个概念，Rectified Flow 是"冒泡排序"。

### Flow Matching 框架下的多种实例

| 实例 | 路径形式 | 特点 |
|:---|:---|:---|
| **Rectified Flow** | 强制直线 $x_t = (1-t)x_0 + tx_1$ | 最简单、最暴力 |
| Diffusion Flow Matching | 复现 DDPM 的 VP/VE 弯曲路径 | 证明 Diffusion 是 FM 的特例 |
| Schrödinger Bridge | 两个分布间最可能的随机演化路径 | FM 的广义随机版本 |
| OT-CFM | 直线 + 训练时用最优传输配对 | 工程上最常用的实践 |

### Rectified Flow 的两个核心特征

**特征一：强制直线路径**

$$x_t = (1-t)x_0 + tx_1, \quad x_0 \sim p_\text{data}, \quad x_1 \sim \mathcal{N}(0,I)$$

这是所有可能路径中最简单的连接方式。

**特征二：Reflow 机制（理论）/ OT 配对（工程实践）**

- **理论上的 Reflow**：最初训练时，噪声和图像是独立随机配对的，导致不同样本的生成轨迹在空间中严重交叉。轨迹交叉意味着同一个 $x_t$ 位置对应多个不同速度方向，ODE 求解器必须用很小步长。

  Reflow 做法：用训练好的模型生成图像，记录噪声→图像的确定性映射对，再训练新模型。数学上等价于寻找最优传输解，显著减少交叉。

- **工程实践**（SD3、Flux 采用）：迭代 Reflow 成本太高（需先生成几十亿张图）。实际做法是训练时直接用**最优传输算法在 batch 内配对**噪声和图像（OT-CFM），单阶段训练就能学到几乎不交叉的直线轨迹。

### 关键区别总结

| | Flow Matching | Rectified Flow |
|:---|:---|:---|
| 层级 | 理论框架 | 框架下的具体实现 |
| 路径限制 | 不限制（可弯曲、可随机） | 强制直线 |
| 速度场 | 任意可积向量场 | $v(x_t, t) \approx x_1 - x_0$（常数方向） |
| 轨迹交叉处理 | 不强制 | Reflow / OT 配对 |
| 理论保证 | 通用 | 直线路径使少步采样成为可能 |

### 为什么 Rectified Flow 在工程中胜出

1. **路径最短**：直线是两点间最短路径，ODE 求解误差最小
2. **速度场简单**：理想情况下速度是常数，网络学习难度低
3. **少步生成**：轨迹不交叉时，1~4 步就能生成高质量图像（Flux [schnell]）
4. **实现简单**：训练损失就是 MSE，没有复杂的 schedule 设计

---

## Q8: 强化学习中 PPO 的 loss 为什么要做 Clip？

### 根本原因：策略梯度的"步子迈太大"会导致训练崩溃

PPO 做 clip 的根本原因是解决策略梯度方法中**更新步长失控**的问题。这个问题有三个逐层叠加的层面：

**第一层：优势估计失效。** 优势 $\hat{A}$ 是在旧策略 $\pi_\text{old}$ 下估计的，前提是"策略不变"。如果一次更新让策略变化太大（比如某动作概率从 70% 跳到 99%），$\hat{A}$ 赖以成立的前提就被打破了。旧的优势值成了远超真实值的错误信号，推动参数进一步偏离。

**第二层：参数空间与策略空间的鸿沟。** 学习率 $\alpha$ 控制的是参数 $\theta$ 在欧几里得空间的幅度，但策略 $\pi_\theta$ 通过 softmax 非线性映射出来。参数空间中大小相同的一步，在策略空间引起的变化可能截然不同。没有固定学习率能在所有方向上都安全。

**第三层：正反馈崩溃循环（RL 独有）。** 在 RL 中训练数据由策略自身产生。一次大步更新 → 策略变差 → 垃圾数据 → 更差梯度 → 恶性循环，训练不可逆转崩溃。

### TRPO 的方案及代价

TRPO 用 KL 散度硬约束：$\text{s.t.} \quad \text{KL}(\pi_\text{old} \| \pi_\theta) \leq \delta$，需要 Fisher 信息矩阵（二阶）来精确求解参数空间与策略空间映射。对百亿参数模型计算代价不可接受。

### PPO Clip 的核心设计

$$\mathcal{L}^{\text{CLIP}}(\theta) = \mathbb{E}_t\left[\min\left(r_t(\theta)\hat{A}_t,\ \text{clip}(r_t(\theta), 1-\epsilon, 1+\epsilon)\hat{A}_t\right)\right]$$

其中 $r_t = \pi_\theta / \pi_{\theta_\text{old}}$ 是重要性采样比率，$\epsilon = 0.2$。

**分情况讨论**：

- $\hat{A}_t > 0$（好动作）：$r_t > 1.2$ 时裁剪生效，梯度为 0——"见好就收"
- $\hat{A}_t < 0$（坏动作）：$r_t < 0.8$ 时裁剪生效，梯度为 0——"防矫枉过正"

取 min 构建"悲观下界"——永远不会因 r 超出范围而获得额外收益，消除了优化器无限放大好动作概率的激励。

### 为什么 Clip 比 KL 惩罚更好

KL 惩罚方案（$L = r\hat{A} - \beta\cdot\text{KL}$）的核心缺陷：$\beta$ 没法确定，同一个值在不同训练阶段效果截然不同——reward 尺度在变、策略曲率在变。太大则学不动，太小则形同虚设。

Clip 的 $\epsilon$ 是 scale-free 的：不管 reward 尺度如何变化，$\epsilon=0.2$ 始终翻译为"概率变化不超约 20%"。天然适应各种训练阶段，不需要动态调整。PPO 原论文实验（Table 1）：PPO-Clip（归一化得分 0.82）显著优于 PPO-Penalty（最高 0.74）。

### Clip 的附带好处

$r \in [1-\epsilon, 1+\epsilon]$ 同时将重要性权重限制在安全范围 → 防止 IS 方差爆炸 → 同一批数据可安全做多个 epoch 更新（通常 3~10 个）→ 样本效率大幅提升（对比 REINFORCE 每批数据只用一次就丢弃）。

### 总结

**PPO Clip 的本质：用一阶优化 + 比率截断，实现 TRPO 的核心目标（限制策略空间更新幅度），避免了二阶计算代价和 KL 系数调节困难。仅用标准 Adam 优化器就能获得与 TRPO 相当的训练稳定性，成为大模型 RLHF 的基石算法。**

---

## Q9: 描述一下什么是 TRPO，有什么优缺点？

### 什么是 TRPO

TRPO（Trust Region Policy Optimization，信任域策略优化）是 Schulman 等人在 2015 年提出的策略优化算法。核心思想：**给策略更新画一个"信任圆圈"——每次只允许在这个圈圈里找最优解，保证永远不会迈太大步。**

### 优化目标

$$\max_\theta \; \mathbb{E}_t\left[\frac{\pi_\theta(a_t|s_t)}{\pi_{\theta_\text{old}}(a_t|s_t)} \hat{A}_t\right] \quad \text{s.t.} \quad \mathbb{E}_t[\text{KL}(\pi_{\theta_\text{old}} \| \pi_\theta)] \leq \delta$$

包含两个关键组件：

1. **替代目标函数**：利用重要性采样，用旧策略数据评估新策略效果。与真实目标在旧策略处相切（值相等、梯度相同），邻域内可靠
2. **KL 散度约束**：直接在策略空间限制更新幅度，$\delta$ 通常 0.01

### 核心贡献：精确求解参数空间与策略空间的映射

TRPO 最深刻的理论贡献：从数学上精确求解了参数空间与分布空间的映射关系。

$$\text{KL} \approx \frac{1}{2}\Delta\theta^T \mathbf{F} \Delta\theta$$

$\mathbf{F}$ 是 Fisher 信息矩阵（KL 对参数的二阶导数），描述"策略空间的局部曲率"。利用这个二阶近似，解析解为：

$$\Delta\theta^* = \sqrt{\frac{2\delta}{g^T\mathbf{F}^{-1}g}} \cdot \mathbf{F}^{-1}g$$

即沿自然梯度方向更新，步长由约束 $\delta$ 精确决定。工程上用共轭梯度法计算 $\mathbf{F}^{-1}g$，无需显式存储 Fisher 矩阵。

### 优点

1. **理论保证**：数学上证明每次更新后策略性能单调不降
2. **解决参数-策略空间错位**：用 Fisher 矩阵精确掌控"地形曲率"，不依赖启发式学习率
3. **步长自动确定**：由约束 $\delta$ 和当前曲率推导，无需手动调学习率
4. **训练稳定**：相比 REINFORCE 剧烈震荡，TRPO 快速稳定收敛

### 缺点

1. **计算代价高**：需多次反向传播计算二阶信息（Fisher-向量乘积），对百亿参数模型不可接受
2. **实现复杂**：共轭梯度法 + 线搜索，代码量远大于标准梯度下降
3. **不兼容标准优化器**：不能使用 Adam/SGD，必须自己实现更新规则
4. **仍需 Critic 网络**：是 Actor-Critic 框架，需要额外的价值网络
5. **小批量效率低**：每次更新需 CG 迭代，难以像 PPO 用 mini-batch 多次复用数据

### 与 PPO 的关系

PPO 继承了 TRPO "限制策略空间更新幅度"的思想，但用 clip 裁剪替代二阶优化：

- 丢失了理论精确解
- 换来了一阶梯度 + Adam、任何网络兼容、代码极简
- 实验性能相当甚至更优，工程友好度远超 TRPO
- 因此 PPO 成为实际使用的主流算法

---

## Q10: 什么是强化学习里面的信用分配问题？

### 定义

信用分配问题（Credit Assignment Problem）：**当智能体在长轨迹结束后才获得总奖励时，如何判断哪些动作对最终结果贡献最大（或最差）？**

### 直觉例子

- **面试**：3 轮面试结束只得"通过/不通过"，哪轮表现最关键？
- **围棋**：200 手后才知赢输，哪手是关键好棋？
- **图像生成**：25 步去噪后才由 RM 打分，哪步决定了画面质量？

### 为什么是难题

1. **奖励稀疏**：很多任务只在终点给奖励，中间步奖励为零
2. **因果链长**：早期决策可能很多步后才产生影响
3. **随机性干扰**：好决策也可能因后续随机事件导致坏结果

### 在各场景中的表现

- **LLM RLHF**：500 Token 回答，只有最终总分。GRPO 将总分平摊给所有 Token——粗糙但实用
- **Flow-GRPO / 图像生成**：25 步去噪，t=0 才有 RM 分数。累积整条轨迹对数概率进行更新
- **MixGRPO 的隐式信用分配**：滑动窗口只在构图阶段探索，假设早期步贡献更大（实验验证前 30% 步贡献最大）

### 经典解决方案

| 方法 | 思路 | 优劣 |
|:---|:---|:---|
| 蒙特卡洛回报 $G_t$ | 从 t 到终点的实际累积 | 无偏但方差极大 |
| TD 学习 | Critic 估计"应得"vs"实际"之差 | 低方差但有偏 |
| GAE | 多步 TD 误差的指数加权和 | PPO/TRPO 标准做法，平衡偏差与方差 |
| GRPO 组内标准化 | 同 Prompt 多回答对比 | 去掉 Critic，信用分配较粗糙 |

### 总结

**信用分配的本质：在稀疏奖励下，如何把"一笔总账"公平分配到"每一步行动"。** 不同算法（PPO 用 GAE、GRPO 用组内标准化、MixGRPO 用滑动窗口）都是不同方式近似解决这个问题。

---
---

# 附录：大模型面试高频问题及回答

## Q11: RLHF 的训练流程分哪几步？

RLHF 分三个阶段：

**阶段一：SFT（监督微调）**——用高质量问答数据做监督学习，教模型"怎么回答"。

**阶段二：RM（奖励模型训练）**——对同一问题生成多个回答，让人类标注哪个更好（偏好数据），训练奖励模型。核心是 Bradley-Terry 损失：$\mathcal{L} = -\log\sigma(r_w - r_l)$。

**阶段三：RL（强化学习优化）**——让模型自己生成回答，RM 当裁判打分，用 PPO/GRPO 算法根据分数更新策略。PPO 阶段需同时驻留四个模型：Actor、Critic、Reference、Reward Model。

---

## Q12: 奖励模型（RM）的训练方法和数据收集方式？

**数据收集**：给同一 Prompt 生成多个回答，人类标注相对排序（A>B>C）。三大范式：人工标注（金标准但贵）、用户隐式反馈（点赞/复制行为）、AI 生成对比（用强模型当标注员）。

**训练方法**：基于 SFT 模型，移除词表层，替换为输出标量的线性头。读取最后一个 token 的隐藏状态做全局语义汇总打分。损失为 BT 损失：$\mathcal{L} = -\log\sigma(r_\theta(x,y_w) - r_\theta(x,y_l))$。

**四种粒度**：序列级 BT-RM（主观对话）→ 结果级 ORM（客观任务）→ 过程级 PRM（每步打分，标注极贵）→ 可验证奖励（规则判定，零成本）。

---

## Q13: 价值模型（Critic/VM）的本质与作用？为什么不能直接用 RM 替代？

**本质**：Critic 估计"从当前状态出发、按当前策略走到底的期望累积奖励" $V(s_t)$，作为优势函数的基线。

**为什么不能用 RM 替代**：RM 是冻结的，只在训练前训练一次；而 Critic 需要随策略一起更新——因为策略在变，"平均表现"也在变。如果用冻结的 RM 当基线，随着策略越来越强，RM 给的基线不再准确，优势估计方差会增大。

GRPO 的做法是用"同组多个回答的均值"替代 Critic——这个均值是实时采样的，自然跟随策略变化，因此不需要单独训练的 Critic。

---

## Q14: DPO 和 PPO/GRPO 的区别？

| | DPO | PPO/GRPO |
|:---|:---|:---|
| 学习方式 | 离线（固定偏好数据集） | 在线（模型自己生成 + 打分） |
| 需要 RM | 不需要（直接从偏好数据学） | 需要（或规则验证） |
| 核心公式 | $\mathcal{L} = -\log\sigma(\beta(\log\frac{\pi_\theta(y_w)}{\pi_\text{ref}(y_w)} - \log\frac{\pi_\theta(y_l)}{\pi_\text{ref}(y_l)}))$ | 裁剪策略梯度 |
| 显存 | 低（只需 Actor + Reference） | 高（PPO 需 4 个模型；GRPO 需 Actor + Reference） |
| 能否"顿悟" | 不能（只死记标准答案） | 能（探索→发现新解法→自我进化） |
| 适用场景 | 有大量高质量偏好数据 | 有可验证的奖励信号（如数学/代码） |

**核心区别**：DPO 是监督学习（学已有答案），PPO/GRPO 是在线试错（自己探索新答案）。

---

## Q15: 什么是 Reward Hacking？如何缓解？

**定义**：RL 优化器找到提升代理 RM 分数的"捷径"，而非真正提升质量。例如 RM 偏好长文，模型就生成无意义冗长内容骗取高分。

**表现**：代理 RM 分数飙升，但真实人类偏好呈"倒 U 型"——先升后降。

**缓解方案**：

1. **KL 约束**：限制策略不偏离参考模型太远（但 Kwa 等发现对重尾误差效果有限）
2. **更强的 RM**：GenRM（生成式，输出推理链后判决，更难被欺骗）、ArmoRM（多维度分别打分）
3. **可验证奖励**：数学/代码用规则判对错，完全无法 hack
4. **定期更新 RM**：随策略进化重新标注训练
5. **DAPO 移除 KL + Clip-Higher**：裁剪机制本身约束更新幅度，避免 KL 的全局压制

---

## Q16: KL 散度在 RLHF 中的作用？

KL 散度约束策略 $\pi_\theta$ 不偏离参考模型 $\pi_\text{ref}$ 太远，有两个层面的作用：

1. **防止 Reward Hacking**：不让模型跑到 RM 误差大的区域去钻漏洞
2. **保持基础能力**：SFT 模型的通用语言能力不因 RL 而退化

实现方式：在 token 级将 KL 惩罚折入奖励 $r'_t = r_t - \beta \cdot (\log\pi_\theta(a_t|s_t) - \log\pi_\text{ref}(a_t|s_t))$，等价于在目标函数中加 KL 正则项。

DAPO 移除 KL 的原因：Clip-Higher 已足够约束单步变化，KL 反而压制有益探索；移除还省掉冻结 Reference Model 的显存。

---

## Q17: DDPM 的前向和反向过程？

**前向（加噪）**：$x_t = \sqrt{1-\beta_t}\,x_{t-1} + \sqrt{\beta_t}\,\epsilon_t$，递推展开得 $x_t = \sqrt{\bar\alpha_t}\,x_0 + \sqrt{1-\bar\alpha_t}\,\epsilon$。方差 $\beta$ 从 $10^{-4}$ 线性增到 $0.02$——起始小噪声保留信息，后期大噪声彻底破坏。

**反向（去噪）**：训练网络 $\epsilon_\theta(x_t, t)$ 预测噪声，采样时：$x_{t-1} = \frac{1}{\sqrt{\alpha_t}}(x_t - \frac{\beta_t}{\sqrt{1-\bar\alpha_t}}\epsilon_\theta) + \sigma_t z$。将"一步生成"拆成"千步微小去噪"。

---

## Q18: DDIM 和 DDPM 的区别？

**核心区别**：DDPM 利用马尔可夫假设，去噪只能在相邻步之间；DDIM 去掉马尔可夫假设，用待定系数法推导出可跨步的去噪公式。

**DDIM 的做法**：保持边际分布 $q(x_t|x_0)$ 不变，但允许从 $x_t$ 直接跳到任意 $x_\tau$（$\tau < t$）。通过待定系数法确定均值和方差，使跳步后仍满足正确的边际分布。

**结果**：DDIM 20~100 步就能达到 DDPM 1000 步的效果，提速 10~50 倍。且当方差设为 0 时变成完全确定性过程（ODE），采样结果可复现。

---

## Q19: Classifier Guidance 和 Classifier-Free Guidance 的区别？

**Classifier Guidance**：训练一个独立分类器 $p(y|x_t)$，用其梯度引导去噪方向：$\hat\epsilon = \epsilon_\theta - s\cdot\sigma_t\nabla_{x_t}\log p(y|x_t)$。需要额外训练噪声鲁棒的分类器。

**Classifier-Free Guidance (CFG)**：不需要分类器。训练时随机 drop 条件（以一定概率把条件设为空），推理时用条件和无条件预测的差异来增强引导：$\hat\epsilon = \epsilon_\text{uncond} + s\cdot(\epsilon_\text{cond} - \epsilon_\text{uncond})$。

**CFG 优势**：无需额外模型，只需一个模型两次前向；效果更好；已成为 SD/Flux 的标准做法。

---

## Q20: Flux 的架构创新？

1. **双流+单流混合 Transformer**：前 19 层双流（文本/图像各自有独立 Attention），后 38 层单流（文本图像拼接做联合 Attention）
2. **Pixel Unshuffle**（无参数）：替代传统 Patch Embedding 卷积，将 2×2 空间邻域折叠进通道维度
3. **RoPE 位置编码**：旋转位置编码，支持可变分辨率
4. **AdaLN-Zero**：全局条件向量（时间步+指引强度+CLIP pooled）通过自适应层归一化调制每层特征
5. **双文本编码器**：CLIP-L（全局语义）+ T5-XXL（细粒度长文本）
6. **12B 参数量**：暴力 Scaling 提升复杂物理/空间/长文本遵循能力

---

## Q21: VAE 的 ELBO 推导？

目标：最大化对数似然 $\log p_\theta(x)$，但直接计算需要遍历所有潜变量 $z$。

引入近似后验 $q_\phi(z|x)$ 后：

$$\log p(x) = \text{ELBO} + \text{KL}(q(z|x) \| p(z|x)) \geq \text{ELBO}$$

$$\text{ELBO} = \mathbb{E}_{q(z|x)}[\log p(x|z)] - \text{KL}(q(z|x) \| p(z))$$

两项的含义：第一项是重建质量（解码器把 $z$ 还原成 $x$ 的能力），第二项是正则化（近似后验不偏离先验太远）。

变分推断的核心：用参数化的已知分布 $q_\phi(z|x)$ 近似真实后验 $p(z|x)$。

---

## Q22: 重要性采样原理？

$\mathbb{E}_{x\sim p}[f(x)] = \mathbb{E}_{x\sim q}[\frac{p(x)}{q(x)}f(x)]$。用旧分布 $q$ 的样本，通过乘以权重 $\frac{p}{q}$ 来无偏估计新分布 $p$ 下的期望。

PPO 中 $r_t = \frac{\pi_\theta}{\pi_\text{old}}$ 就是重要性权重，使旧策略采集的数据能用于评估新策略。

**IS 方差爆炸**：当新旧分布差距大时权重方差极大。PPO 的 Clip 机制将 $r$ 限制在 $[0.8, 1.2]$，同时解决了步长控制和方差控制。

---

## Q23: 策略梯度高方差问题及解决方案？

**高方差来源**：$\nabla J = \mathbb{E}[\nabla\log\pi \cdot G_t]$ 中 $G_t$ 包含从当前步到终点的所有随机性。

**解决方案**（从粗到精）：

1. **减去 Baseline** $b(s)$：$G_t - b$ 在零附近波动，方差降低。理论最优 baseline 是 $V(s)$
2. **Actor-Critic**：用 Critic $V_\phi(s)$ 实时估计 baseline，不用等到轨迹结束
3. **GAE**：多步 TD 误差的指数加权和，用 $\lambda$ 参数平衡偏差和方差

---

## Q24: On-policy vs Off-policy？

| | On-policy | Off-policy |
|:---|:---|:---|
| 数据来源 | 必须用当前策略采集 | 可用任意策略采集的数据 |
| 代表算法 | PPO、TRPO、GRPO | DQN、SAC、DPO |
| 样本效率 | 低（数据用完即丢） | 高（可复用历史数据） |
| 稳定性 | 高（数据分布匹配） | 低（需要修正分布偏差） |

PPO 通过 IS + Clip 实现了"伪 Off-policy"——同一批数据复用 K 个 epoch，但仍需约束在旧策略附近。

---

## Q25: 什么是 GAE？

GAE（广义优势估计）：$\hat{A}_t^{\text{GAE}} = \sum_{l=0}^{\infty}(\gamma\lambda)^l \delta_{t+l}$，其中 $\delta_t = r_t + \gamma V(s_{t+1}) - V(s_t)$ 是 TD 误差。

$\lambda$ 参数的作用：

- $\lambda = 0$：退化为单步 TD，低方差高偏差
- $\lambda = 1$：退化为蒙特卡洛回报，高方差零偏差
- $\lambda \in (0,1)$：平衡两者，PPO 常用 $\lambda = 0.95$

---

## Q26: 前向 KL vs 反向 KL？

$$\text{KL}(p\|q) = \mathbb{E}_p[\log\frac{p}{q}] \quad \text{（前向，mean-seeking）}$$
$$\text{KL}(q\|p) = \mathbb{E}_q[\log\frac{q}{p}] \quad \text{（反向，mode-seeking）}$$

- **前向 KL**（$p$ 为真实）：$q$ 必须覆盖 $p$ 的所有模式，否则 $p$ 有密度而 $q$ 为零处惩罚无穷大。结果 $q$ 偏向"平均"所有模式（模糊但全面）
- **反向 KL**（$q$ 为近似）：$q$ 只需覆盖 $p$ 的某个模式即可，未覆盖部分惩罚为零。结果 $q$ 偏向"锁定"最强模式（清晰但可能丢失多样性）

RLHF 中用 $\text{KL}(\pi_\theta \| \pi_\text{ref})$（反向）：策略被约束不偏离参考模型太远，保持模式一致性。
